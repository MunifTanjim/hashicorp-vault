{
  "method": "post",
  "summary": "This endpoint closes a connection and it's underlying plugin and restarts it with the configuration stored in the barrier.",
  "path": "/database/reset/:name",
  "parameters": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": "Specifies the name of the connection to reset."
    }
  ]
}
