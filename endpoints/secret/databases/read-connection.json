{
  "method": "get",
  "summary": "This endpoint returns the configuration settings for a connection.",
  "path": "/database/config/:name",
  "parameters": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": "Specifies the name of the connection to read."
    }
  ]
}
