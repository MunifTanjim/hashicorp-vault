{
  "method": "post",
  "summary": "This endpoint is used to rotate the root superuser credentials stored for the database connection.",
  "path": "/database/rotate-root/:name",
  "parameters": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": "Specifies the name of the connection to rotate."
    }
  ]
}
