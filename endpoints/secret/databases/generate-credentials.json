{
  "method": "get",
  "summary": "This endpoint generates a new set of dynamic credentials based on the named role.",
  "path": "/database/creds/:name",
  "parameters": [
    {
      "name": "name",
      "type": "string",
      "required": true,
      "description": "Specifies the name of the role to create credentials against."
    }
  ]
}
