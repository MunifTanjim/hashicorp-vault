{
  "method": "list",
  "summary": "This endpoint returns a list of key names at the specified location.",
  "path": "/:kvDir/:path",
  "parameters": [
    {
      "name": "kvDir",
      "type": "string",
      "description": "KV mount directory."
    },
    {
      "name": "path",
      "type": "string",
      "required": true,
      "description": "Specifies the path of the secrets to list."
    }
  ]
}
