{
  "method": "delete",
  "summary": "This endpoint deletes the secret at the specified location.",
  "path": "/:kvDir/:path",
  "parameters": [
    {
      "name": "kvDir",
      "type": "string",
      "description": "KV mount directory."
    },
    {
      "name": "path",
      "type": "string",
      "required": true,
      "description": "Specifies the path of the secret to delete."
    }
  ]
}
