{
  "method": "put",
  "summary": "This endpoint stores a secret at the specified location.",
  "path": "/:kvDir/:path",
  "parameters": [
    {
      "name": "kvDir",
      "type": "string",
      "description": "KV mount directory."
    },
    {
      "name": "path",
      "type": "string",
      "required": true,
      "description": "Specifies the path of the secrets to create/update."
    },
    {
      "name": "key",
      "type": "object",
      "required": false,
      "default": "",
      "description": "Specifies a key, paired with an associated value, to be held at the given location."
    }
  ]
}
